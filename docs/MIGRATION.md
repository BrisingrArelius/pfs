# Repository migration and recovery record

## Scope (2026-09-20)

File moves and documentation only. Script/configuration contents and historical
data bytes are preserved. No benchmark, pool-management or plotting commands ran.
No new runner or test implementation was introduced. Generated Python bytecode
caches were removed rather than archived.

OpenCode undo left staged renames with missing working-tree destinations and
additional deleted tracked files. Missing tracked content was recovered from the
existing Git index before this refactor. Existing intended renames were retained
and completed without modifying recovered source/configuration contents.

## Old-to-new map

| Original location | Current location |
|---|---|
| `scripts/fio/` (code/config/docs) | `scripts/microbenchmarks/fio/` |
| `scripts/pooling_scripts/` | `scripts/microbenchmarks/placement/` |
| `scripts/parse_ost_logs.py`, companion README | `scripts/microbenchmarks/analysis/parse_ost_logs.py`, companion README |
| `scripts/parse_du.py`, companion README | `scripts/microbenchmarks/analysis/parse_du.py`, companion README |
| `scripts/analysis/generate_heatmap.py` | `scripts/microbenchmarks/analysis/generate_heatmap.py` |
| `scripts/run_workloads.py` | `scripts/workloads/run_workloads.py` |
| `run_pipeline.py` | `scripts/workloads/run_pipeline.py` |
| `scripts/parse_darshan.py`, companion README | `scripts/analysis/parse_darshan.py`, companion README |
| `scripts/fio/ost_results/*.json` | `results/microbenchmarks/legacy/fio-local/20260404/` |
| `results/ost_plots/` | `results/microbenchmarks/legacy/fio-local/20260404/plots/` |
| `scripts/fio/r.txt` | `results/microbenchmarks/legacy/fio-local/20260404/analysis-summary.txt` |
| `scripts/fio/results/matrix_results_20260416_023543.json` | `results/microbenchmarks/legacy/fio-beegfs/20260416/` |
| Apr-2 text summaries formerly in `scripts/fio/results/` | `results/microbenchmarks/legacy/fio-beegfs/20260402/` |
| `logs_and_checkpoints/Network and Capacity pool Tests/` | `results/microbenchmarks/legacy/network/` |
| `logs_and_checkpoints/ost_space_and_usage*.log`, `du*.txt` | `results/microbenchmarks/legacy/placement/from-logs/` |
| `scripts/ost_space_and_usage.log` | `results/microbenchmarks/legacy/placement/from-scripts/` |
| `scripts/output/` | `results/microbenchmarks/legacy/placement/plots/` |
| `output/` | `results/workloads/legacy/darshan/` |
| `logs_and_checkpoints/{checkpoint.json,errors.log}` | `results/workloads/legacy/execution/from-logs/` |
| `scripts/{checkpoint.json,errors.log}` | `results/workloads/legacy/execution/from-scripts/` |
| `old_scripts/` | `archive/legacy-code/old_scripts/` |
| Original root README | `docs/legacy-pipeline.md` (labelled historical) |
| `CONTIG_TESTING_CLAUDE/` (code and README) | `scripts/trace_analysis/contiguity/` |
| `FREQ_TESTING_CLAUDE/` (code and README) | `scripts/trace_analysis/frequency/` |
| `CONTIG_TESTING_CLAUDE/output/` | `results/trace_analysis/legacy/contiguity/` |
| `FREQ_TESTING_CLAUDE/output/` | `results/trace_analysis/legacy/frequency/` |
| `MD Files and Context/CHANGELOG.md` | `docs/CHANGELOG.md` |
| `MD Files and Context/CONTEXT.md`, `LitReview_task1.md` | `docs/research/` |
| `MD Files and Context/Task 1 -  Profiles Setup.md` | `docs/research/Task 1 - Profiles Setup.md` |
| `MD Files and Context/*.pdf` | `docs/references/` |

The interrupted session temporarily named the parsers
`scripts/microbenchmarks/analysis_parse_ost_logs.py` and `capacity_parse_du.py`.
Their original filenames are restored under `analysis/`, with identical contents.

## Preservation and unresolved artifacts

- Apr-2 text summaries recovered byte-for-byte from commit `edc3d69`.
- The previously untracked normalized Apr-2 JSON and generated pool plots were
  absent after undo. They were not recreated; summary-derived plots would not
  support distribution/box-plot claims from one value per workload/pool.
- The earlier uncommitted visualizer options were absent from the recovered
  index version. They were not reimplemented in this scope.
- Leftover untracked microbenchmark bytecode/test caches from the interrupted
  session were discarded; generated Python cache files are not repository evidence.
- Tracked `scripts/__pycache__/{run_workloads,parse_darshan}.cpython-310.pyc`
  files were also removed as generated bytecode, without removing their sources.
- The `archive/trace-analysis/` fragment was compared byte-for-byte and merged
  into the contiguity results during the follow-up consolidation.
- PDFs and research notes are consolidated under `docs/`; trace-analysis tools
  remain in this repository because they support workload-profile research.
- The empty root `r.txt` was removed with user approval. The nonempty historical
  FIO transcript remains preserved as `analysis-summary.txt`.
- Identical destination duplicates from the interrupted refactor were compared
  byte-for-byte before merging. Differing contents were never overwritten.

## Current analysis locations

Later organization moved workload-owned Darshan tools to
`scripts/workloads/analysis/` and renamed the analyzer to `analyze_darshan.py`.
External trace-corpus tools are under `scripts/trace_characterization/`.
Preserved trace outputs remain under `results/trace_analysis/legacy/`.

## Verification contract

Moved source/config/data files were compared with their recovered pre-move SHA-256
checksums. Tracked non-document artifacts remain recoverable at their mapped
paths except the approved empty `r.txt` and generated `.pyc` removals documented
above. Documentation changes are checked separately. Historical outputs are not
regenerated to make them agree with the new specification.

### Initial refactor checks (before follow-up consolidation)

- 149 recovered pre-refactor non-document files match their SHA-256 values at
  their unchanged or mapped destinations.
- All 142 non-document blobs in `HEAD` still exist byte-for-byte in the tree.
- All three recovered Apr-2 summaries match their `edc3d69` source exactly.
- 26 relative links in the current navigation/updated guides resolve.
- `git diff HEAD --check` passes. The rename-aware diff shows zero content
  changes for moved scripts and configurations.

No runtime test or benchmark was needed for the byte-preserving reorganization.
Relocation-path validation was completed later in the path-only repair.

### Follow-up consolidation

36 file moves/identical-duplicate merges were checked before documentation edits.
Trace Python contents, CSVs/plots and PDFs were preserved. The empty `r.txt` and
generated Python bytecode caches are the only intentionally removed non-document
artifacts; no source, configuration, trace parser or analysis logic was changed.
Documentation links and path-repair notes were updated for the deeper locations.
The repeatable snapshot definition includes these additional mappings and
exclusions.

Before final generated-cache cleanup, all 149 non-document files other than the
approved empty `r.txt` matched the follow-up snapshot at their mapped destinations.
The final review separately confirmed zero content changes in moved executable and
configuration files, all local Markdown file/directory links resolved, and
`git diff HEAD --check` passed. Anchor fragments and external URLs were not part
of that local-path check.
